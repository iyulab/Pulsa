name: Release PulsaLLM

on:
  push:
    tags: ['llm-v*']

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Extract version
        id: version
        shell: bash
        run: echo "version=${GITHUB_REF_NAME#llm-v}" >> $GITHUB_OUTPUT

      - name: Publish
        run: >
          dotnet publish src/PulsaLLM/PulsaLLM.csproj
          -c Release
          -r win-x64
          --self-contained
          -p:PublishSingleFile=true
          -p:Version=${{ steps.version.outputs.version }}
          -o publish/PulsaLLM

      - name: Package
        shell: bash
        run: cd publish && 7z a -tzip PulsaLLM-win-x64.zip PulsaLLM/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: PulsaLLM v${{ steps.version.outputs.version }}
          files: publish/PulsaLLM-win-x64.zip
