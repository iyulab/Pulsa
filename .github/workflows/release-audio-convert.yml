name: Release PulsaAudioConvert

on:
  push:
    tags: ['audio-convert-v*']
  workflow_call:
  workflow_dispatch:

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Extract version
        id: version
        shell: bash
        run: |
          version=$(sed -n 's/.*<Version>\([^<]*\)<.*/\1/p' src/workers/Pulsa.AudioConvert.Worker/PulsaAudioConvert.Worker.csproj)
          echo "version=${version}" >> $GITHUB_OUTPUT

      - name: Publish
        run: >
          dotnet publish src/workers/Pulsa.AudioConvert.Worker/PulsaAudioConvert.Worker.csproj
          -c Release
          -r win-x64
          --self-contained
          -p:PublishSingleFile=true
          -p:DebugType=none
          -o publish/PulsaAudioConvert

      - name: Package
        shell: bash
        run: cd publish/PulsaAudioConvert && 7z a -tzip ../PulsaAudioConvert-win-x64.zip .

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: audio-convert-v${{ steps.version.outputs.version }}
          name: PulsaAudioConvert v${{ steps.version.outputs.version }}
          files: publish/PulsaAudioConvert-win-x64.zip

      - name: Cleanup old releases
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release list --json tagName -q '.[].tagName' \
            | grep '^audio-convert-v' | sort -V | head -n -5 \
            | while read tag; do
                echo "Deleting release $tag"
                gh release delete "$tag" --yes --cleanup-tag
              done
