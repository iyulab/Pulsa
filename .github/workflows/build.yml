name: Build

on:
  push:
    branches: [main]
    paths: ['src/**']
  pull_request:
    branches: [main]
    paths: ['src/**']
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        project:
          - src/tools/PulsaSTT/PulsaSTT.csproj
          - src/tools/PulsaAudioConvert/PulsaAudioConvert.csproj
          - src/tools/PulsaLLM/PulsaLLM.csproj
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Build
        run: dotnet build ${{ matrix.project }} -c Release
