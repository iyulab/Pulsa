name: Release PulsaVault

on:
  push:
    tags: ['vault-v*']
  workflow_call:
  workflow_dispatch:

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Extract version
        id: version
        shell: bash
        run: |
          version=$(sed -n 's/.*<Version>\([^<]*\)<.*/\1/p' src/workers/Pulsa.Vault.Worker/PulsaVault.Worker.csproj)
          echo "version=${version}" >> $GITHUB_OUTPUT

      - name: Publish
        run: >
          dotnet publish src/workers/Pulsa.Vault.Worker/PulsaVault.Worker.csproj
          -c Release
          -r win-x64
          --self-contained
          -p:PublishSingleFile=true
          -p:DebugType=none
          -o publish/PulsaVault

      - name: Package
        shell: bash
        run: cd publish/PulsaVault && 7z a -tzip ../PulsaVault-win-x64.zip .

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: vault-v${{ steps.version.outputs.version }}
          name: PulsaVault v${{ steps.version.outputs.version }}
          files: publish/PulsaVault-win-x64.zip

      - name: Cleanup old releases
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release list --json tagName -q '.[].tagName' \
            | grep '^vault-v' | sort -V | head -n -5 \
            | while read tag; do
                echo "Deleting release $tag"
                gh release delete "$tag" --yes --cleanup-tag
              done
